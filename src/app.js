import duplicateEncode from "./duplicateEncode.js";
console.log("Successfully started");
const inp = "Success";
const out = duplicateEncode(inp);
console.log(`The result of function duplicateEncode("${inp}") is "${out}".\n`);
